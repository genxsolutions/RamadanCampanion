package com.ramadan.companion.core.designsystem.theme

import androidx.compose.ui.graphics.Color

/**
 * Ramadan Companion AI design system colors.
 * Deep navy and gold theme aligned with design reference.
 */
object RamadanColors {
    // Primary palette - Gold (accent, CTAs, progress)
    val Gold = Color(0xFFE3B96B)
    val GoldDark = Color(0xFFd4a859)
    val GoldDim = Color(0xB3E3B96B) // 70%

    // Background - Deep navy
    val NavyDeep = Color(0xFF0a0f24)
    val NavyPrimary = Color(0xFF0D1B3D)
    val NavyVariant = Color(0xFF1a1533)
    val NavyCard = Color(0xFF1a1e4a)

    // Surface / card tints
    val PurpleAccent = Color(0xFF2B2D6E)
    val PurpleAccentDark = Color(0xFF1a1e4a)

    // Text
    val TextPrimary = Color(0xFFF6F2E8)
    val TextSecondary = Color(0xB3F6F2E8)   // 70%
    val TextTertiary = Color(0x80F6F2E8)    // 50%

    // Overlays
    val OverlayLight = Color(0x14F6F2E8)
    val OverlayGold = Color(0x26E3B96B)     // 15%
    val BorderGold = Color(0x33E3B96B)      // 20%
}
